# Nginx map for WebSocket upgrade handling — required by sites/panel.conf.
# Installed to /etc/nginx/conf.d/upgrade.conf so it's in the http{} context.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
